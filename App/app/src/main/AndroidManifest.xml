<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trulymadly">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrulyMadly">
        <activity android:name=".image"></activity>
        <activity android:name=".Imageone" />
        <activity android:name=".BasicDetailsScreen4" />
        <activity android:name=".AboutYouScreen3" />
        <activity android:name=".FacebookLoginScreen" />
        <activity android:name=".MobileLoginScreen" />
        <activity android:name=".StartScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>